<section class="relative">
  <style>
    .hero-display {
      font-family: "Avenir Next", Avenir, "Trebuchet MS", "Segoe UI", sans-serif;
      letter-spacing: -0.03em;
      line-height: 1.05;
    }

    .word-rotate {
      display: flex;
      flex-direction: column;
      animation: rotateWords 12s infinite;
    }

    .word-rotate span {
      height: 1.2em;
      display: flex;
      align-items: center;
    }

    @keyframes rotateWords {
      0% {
        transform: translateY(0%);
      }

      20% {
        transform: translateY(0%);
      }

      25% {
        transform: translateY(-100%);
      }

      45% {
        transform: translateY(-100%);
      }

      50% {
        transform: translateY(-200%);
      }

      70% {
        transform: translateY(-200%);
      }

      75% {
        transform: translateY(-300%);
      }

      95% {
        transform: translateY(-300%);
      }

      100% {
        transform: translateY(0%);
      }
    }
  </style>

  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-20 sm:py-28">
    <div class="max-w-3xl">
      <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-white/70 backdrop-blur-sm border border-orange-200/60 shadow-sm mb-6">
        <span class="relative flex h-2 w-2">
          <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-orange-400 opacity-75"></span>
          <span class="relative inline-flex rounded-full h-2 w-2 bg-orange-500"></span>
        </span>
        <span class="text-sm font-medium text-orange-900">OAuth 2.0 + OpenID Connect</span>
      </div>

      <h1 class="hero-display text-4xl sm:text-6xl font-bold text-slate-900">
        Identity and OAuth
        <span class="block mt-2">
          <span class="text-transparent bg-clip-text bg-linear-to-r from-orange-600 to-orange-500 relative inline-block h-[1.2em] overflow-hidden align-bottom">
            <span class="word-rotate absolute inset-0">
              <span>designed for modern applications</span>
              <span>built for web platforms</span>
              <span>secure for mobile apps</span>
              <span>optimized for backend systems</span>
            </span>
          </span>
        </span>
      </h1>

      <p class="mt-6 text-base sm:text-lg text-slate-600 max-w-xl leading-relaxed">
        Authura provides OAuth 2.0 and OpenID Connect for secure authentication,
        token management, and session handling across web, mobile, and backend applications.
      </p>

      <div class="mt-8 flex flex-col sm:flex-row gap-4">
        <a href="/signup" class="inline-flex items-center justify-center px-6 py-3 rounded-xl bg-linear-to-r from-sky-500 to-sky-600 text-white text-base font-semibold hover:from-sky-600 hover:to-sky-700 transition-all shadow-lg shadow-sky-500/30">
          Start building
        </a>
        <a href="/signin" class="inline-flex items-center justify-center px-6 py-3 rounded-xl bg-white border border-slate-200 text-slate-700 font-semibold hover:bg-slate-50 transition-all">
          Go to dashboard
        </a>
      </div>

      <div class="mt-10 flex flex-wrap items-center gap-6 text-sm text-slate-500">
        <div class="flex items-center gap-2">
          <span class="w-2 h-2 rounded-full bg-emerald-500"></span>
          <span>Token rotation ready</span>
        </div>
        <div class="flex items-center gap-2">
          <span class="w-2 h-2 rounded-full bg-emerald-500"></span>
          <span>Client management UI</span>
        </div>
        <div class="flex items-center gap-2">
          <span class="w-2 h-2 rounded-full bg-emerald-500"></span>
          <span>OIDC discovery endpoints</span>
        </div>
      </div>
    </div>
  </div>
</section>